import { Route } from '@angular/router';
import { FeaturesCatalog } from './features-catalog/features-catalog';

export const featuresCatalogRoutes: Route[] = [
  { path: '', component: FeaturesCatalog },
];
